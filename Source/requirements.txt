# ---------------------------------------------------------------------------
# Requirements - NVIDIA valuation study data pipelines
# Python 3.10+  (tested on 3.12)
#
# Module dependency map:
#   sec_edgar_ingest.py      ->  pandas, requests
#   yahoo_prices_ingest.py   ->  pandas, yfinance
#   join_quarterly.py        ->  pandas            (built next)
#
# Install:               pip install -r requirements.txt
# Exact reproducible lock after install:
#                        pip freeze > requirements.lock
# ---------------------------------------------------------------------------

pandas>=2.2            # tested 3.0.2  - dataframes, resampling, pivots, joins
requests>=2.31         # tested 2.33.1 - SEC EDGAR companyfacts fetch (User-Agent + retry)
yfinance>=0.2.40       # tested 1.4.1  - Yahoo Finance daily prices / indices
numpy>=1.23.0
#matplotlib>=3.5.0
#statsmodels>=0.14.0  # optional for seasonal decomposition/backups
